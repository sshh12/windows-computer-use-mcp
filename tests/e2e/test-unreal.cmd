@echo off
rem Real-engine e2e: a headless agent drives the Unreal Engine editor purely by sight -
rem add an object, switch to wireframe, then Play (PIE) and Stop.
rem PREREQUISITE: the Unreal Editor (UnrealEditor.exe) must already be open with a project loaded.
call "%~dp0run-mcp.cmd" "Using ONLY the windows-computer-use MCP tools (do not use Bash): The Unreal Engine editor (process UnrealEditor.exe) should be open with a project loaded. First confirm it via the window tool; if it is NOT open, say so clearly and stop. Otherwise, driving the editor by sight (screenshot, then click/type/keys): (1) focus the Unreal Editor window; (2) add a simple object to the level - a Cube - using the toolbar's green Add (+) Actors button or by right-clicking the viewport and choosing Place Actor > Cube, and confirm it appears in the World Outliner; (3) change the viewport view mode to Wireframe (the view-mode dropdown at the top-left of the viewport that reads 'Lit', or try the Alt+2 shortcut); (4) Play the level in the editor (the Play button in the main toolbar), let it run about 3 seconds, then Stop it (the Stop button, or press Esc). Take a screenshot before and after each step to verify. Narrate exactly what you did and whether each step visibly worked."
